{
    "name": "sea-battle",
    "description": "Sea Battle game",
    "addons": ["heroku-postgresql:hobby-dev"],
    "env": {
        "TOKEN_SECRET_KEY": {
            "required": true
        },
        "TOKEN_EXPIRES_IN": {
            "required": true
        }
    }
}
